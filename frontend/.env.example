VITE_API_BASE_URL=http://localhost:4000

# Same campus domain validation as backend REGISTRATION_EMAIL_DOMAIN (shown in UI).
# Default dorsu.edu.ph. Use * to allow any valid email domain (development only).
VITE_REGISTRATION_EMAIL_DOMAIN=dorsu.edu.ph
